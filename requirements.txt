# ============================================================
# MultiBA — Multimodal Binding Affinity Predictor
# Requirements  (Python 3.10+)
# ============================================================

# ── Core Deep Learning ──────────────────────────────────────
torch>=2.3.0
torchvision>=0.18.0
pytorch-lightning>=2.3.0

# ── Transformers & Language Models ──────────────────────────
transformers>=4.41.0        # ESM-2, ChemBERTa-2
fair-esm>=2.0.0             # Facebook ESM-2 (protein LM)
peft>=0.11.0                # LoRA / parameter-efficient fine-tuning
accelerate>=0.30.0          # Multi-GPU / mixed precision

# ── Graph Neural Networks ────────────────────────────────────
torch-geometric>=2.5.3      # PyTorch Geometric (GAT, GIN)
torch-scatter>=2.1.2
torch-sparse>=0.6.18

# ── Chemistry / Cheminformatics ──────────────────────────────
rdkit>=2024.03.1            # SMILES parsing, fingerprints, 3D
deepchem>=2.8.0             # DeepChem featurizers + splitters
molfeat>=0.10.0             # Unified molecular featurization

# ── Data Science ─────────────────────────────────────────────
numpy>=1.26.4
pandas>=2.2.2
scipy>=1.13.0
scikit-learn>=1.5.0

# ── Experiment Tracking ──────────────────────────────────────
mlflow>=2.13.0
wandb>=0.17.0

# ── Explainability ───────────────────────────────────────────
shap>=0.45.0
captum>=0.7.0               # PyTorch interpretability

# ── Visualization ────────────────────────────────────────────
matplotlib>=3.9.0
seaborn>=0.13.2
plotly>=5.22.0
py3Dmol>=2.1.0              # 3D molecular visualization in notebooks

# ── Demo App ─────────────────────────────────────────────────
gradio>=4.37.0
pillow>=10.3.0

# ── Data Access ──────────────────────────────────────────────
kaggle>=1.6.14              # Kaggle API for dataset download
requests>=2.32.0
tqdm>=4.66.4
biopython>=1.83             # Protein sequence utilities

# ── Config Management ────────────────────────────────────────
hydra-core>=1.3.2
omegaconf>=2.3.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0

# ── Utilities ────────────────────────────────────────────────
loguru>=0.7.2
rich>=13.7.1
einops>=0.8.0               # Tensor rearrangement (clean attention code)
